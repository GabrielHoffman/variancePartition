# Test if formula is full rank on this dataset

Test if formula is full rank on this dataset

## Usage

``` r
isRunableFormula(exprObj, formula, data)
```

## Arguments

- exprObj:

  expression object

- formula:

  formula

- data:

  data
